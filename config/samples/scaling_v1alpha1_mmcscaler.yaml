apiVersion: scaling.will-m-0.github.io/v1alpha1
kind: MMcScaler
metadata:
  labels:
    app.kubernetes.io/name: queue-scaler
    app.kubernetes.io/managed-by: kustomize
  name: mmcscaler-sample
spec:
  # TODO(user): Add fields here
